<resources>

    <style name="Base.AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:actionBarSize">@dimen/height_toolbar</item>
    </style>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Base.AppTheme">
        <item name="android:windowDrawsSystemBarBackgrounds">@android:color/transparent</item>
        <item name="android:windowTranslucentStatus">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.LightAppBarOverlay" parent="ThemeOverlay.AppCompat.ActionBar">
        <item name="colorPrimary">#666666</item>
        <item name="colorPrimaryDark">#666666</item>
        <item name="colorAccent">@color/master</item>
        <item name="colorControlNormal">#666666</item>
    </style>

    <style name="ToolBarStyle" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:textSize">16sp</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="MainToolbarTitleText" parent="@style/TextAppearance.Widget.AppCompat.Toolbar.Title"></style>

    <style name="ToolbarTitleText" parent="@style/TextAppearance.Widget.AppCompat.Toolbar.Title"></style>

    <!-- material edit text input -->
    <style name="customEditTextLineColor" parent="Theme.AppCompat.Light">
        <item name="colorControlActivated">@color/colorPrimary</item>
        <item name="colorControlNormal">@color/divider</item>
        <item name="android:textColorHint">@color/color_hint_text</item>
    </style>

    <!-- material edit text input -->
    <style name="customEditTextHint" parent="TextAppearance.AppCompat">
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="android:textSize">@dimen/font_size_body</item>
    </style>

    <!-- single round button -->
    <style name="RoundButtonSingle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">40dp</item>
        <item name="android:textSize">@dimen/font_size_body</item>
        <item name="android:textColor">@color/main_btn_text</item>
        <item name="android:layout_marginLeft">48dp</item>
        <item name="android:layout_marginRight">48dp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:background">@drawable/btn_round_bg</item>
    </style>

    <!-- single button -->
    <style name="ButtonSingle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">40dp</item>
        <item name="android:textSize">@dimen/font_size_body</item>
        <item name="android:textColor">@color/main_btn_text</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:background">@drawable/btn_bg</item>
    </style>

    <!-- single gray button -->
    <style name="ButtonGraySingle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">40dp</item>
        <item name="android:stateListAnimator">@null</item>
        <item name="android:textSize">@dimen/font_size_body</item>
        <item name="android:textColor">@color/gray_btn_text</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:background">@drawable/btn_gray_bg</item>
    </style>

    <!-- single button -->
    <style name="MainButtonSingle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">50dp</item>
        <item name="android:textSize">@dimen/font_size_subheading</item>
        <item name="android:textColor">@color/main_btn_text</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:background">@drawable/btn_bg</item>
    </style>

    <!-- custom progress dialog -->
    <style name="CustomProgressDialogStyle" parent="@style/Theme.AppCompat.Dialog">
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="colorAccent">#BBC0C0C0</item>
    </style>

    <style name="FadeAnimTheme">
        <item name="android:activityOpenEnterAnimation">@android:anim/fade_in</item>
        <item name="android:activityOpenExitAnimation">@android:anim/fade_out</item>
        <item name="android:activityCloseEnterAnimation">@android:anim/fade_in</item>
        <item name="android:activityCloseExitAnimation">@android:anim/fade_out</item>
    </style>

    <style name="FadeTheme" parent="@style/AppTheme">
        <item name="android:windowAnimationStyle">@style/FadeAnimTheme</item>
    </style>

    <style name="anim_bottom_popupwindow">
        <item name="android:windowEnterAnimation">@anim/bottom_popupwindow_in</item>
        <item name="android:windowExitAnimation">@anim/bottom_popupwindow_out</item>
    </style>

    <style name="BrahmaProgressBarSmall" parent="@style/Widget.AppCompat.ProgressBar">
        <item name="android:minWidth">24dp</item>
        <item name="android:maxWidth">24dp</item>
        <item name="android:minHeight">24dp</item>
        <item name="android:maxHeight">24dp</item>
    </style>

    <style name="Theme.AppCompat.Light.Dialog.Alert.Self" parent="@style/Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:windowSoftInputMode">adjustResize</item>
    </style>
</resources>
